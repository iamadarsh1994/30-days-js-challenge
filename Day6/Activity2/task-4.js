//use the 'pop' method to remove the last element from the array and log the updated array
//No Arguments Needed: The pop method does not use any arguments.

let newArr = [1, 2, 3, 4, 5, 9]
newArr.pop();
console.log(newArr)
