//wap that uses the || operator to combine two conditions and log the result.
// Logical OR (||)

let x = 50;
let y = 10;

if (x > 0 || y > 0) {
  console.log("At least one of x or y is a positive number."); 
}else{
    console.log('Hey this is something')
}